name: Build Nuitka Multi-OS

on:
  push:
    branches: [ "main","crud_keyword" ]
  workflow_dispatch: # เพื่อให้กดรันเองได้ด้วย

jobs:
  build:
    strategy:
      matrix:
        # 🟢 หัวใจสำคัญ: สั่งให้ทำงาน 2 ระบบพร้อมกัน
        os: [windows-latest, macos-latest]

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' # หรือเวอร์ชันที่คุณใช้
        cache: 'pip'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # 🟢 ต้องลง cryptography ด้วย ไม่งั้น Nuitka จะหา module ไม่เจอตอน compile
        pip install nuitka cryptography
        # pip install -r requirements.txt # (ถ้ามีไฟล์นี้ให้เอา comment ออก)

    - name: Build Module
      shell: bash
      run: |
        # คำสั่งนี้ใช้ได้ทั้ง Windows และ Mac (ไม่ต้องใส่ --mingw64)
        # Nuitka จะเลือก Compiler ของแต่ละเครื่องให้อัตโนมัติ
        python -m nuitka --module --include-package=modules --output-dir=build modules

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        # ตั้งชื่อไฟล์แยกตาม OS (เช่น built-module-windows-latest)
        name: built-module-${{ matrix.os }}
        path: build/